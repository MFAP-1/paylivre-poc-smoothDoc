{
  "name": "smooth-doc-starter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "develop": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "gatsby build --prefix-paths && github-pages -d public"
  },
  "dependencies": {
    "gatsby": "^2.15.18",
    "gh-pages": "^3.2.3",
    "react": "^16.10.1",
    "react-dom": "^16.10.1",
    "smooth-doc": "^5.0.4"
  }
}
